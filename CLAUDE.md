# CLAUDE.md

このファイルは、このリポジトリで作業する Claude Code (claude.ai/code) への指針です。

## これは何か

3歳半の子ども向けの、ブラウザで遊ぶゲーム集です。Android Chromeで動かすことを想定しています。ビルドシステムやパッケージマネージャ、サーバー、テストの類は一切なく、プロジェクト全体が自己完結した `.html` ファイル数個で構成されています。`file://` で直接開くか、端末に転送してそのまま開いて遊びます。

想定利用シーンは、レストランの待ち時間など**静かに待っていてほしい状況**での「暇つぶし」です。このため、遊んでいるうちに子どもの声や音がエスカレートしうるゲームメカニクス(マイク入力、声の大きさや叫び声への反応など)は採用しないこと。タップ・スワイプなど画面内で完結する静かな操作のみを対象とする。

## コマンド

なし。インストール・ビルド・lint・テストのいずれも不要です。「実行」する際は `index.html` をブラウザで開くか、各ゲームのHTMLファイルを直接開いてください。

## アーキテクチャ

- `index.html` はホーム(目次)ページです。`GAMES` 配列(`{ title, icon, file }`)からカードの一覧を描画し、各カードから通常の `<a href>` で各ゲームのHTMLファイルへリンクします。**新しいゲームを追加する場合は、この配列にエントリを1つ追加するだけで済みます**。それ以外の配線は不要です。
- 各ゲーム(例: `pop-pop-game.html`)は単一の自己完結したHTMLファイルです。`<style>` と `<script>` はすべてインラインで、外部アセットなし、ゲーム間で共有するJS/CSSファイルもなく、ネットワーク通信も行いません。これは、どのファイルもそれ単体でスマホにコピーしてオフラインで開けるようにするための意図的な設計です。

## `pop-pop-game.html` で確立した規約(新しいゲームでもこれに従うこと)

- **キッズプルーフ**: viewportメタで `maximum-scale=1, user-scalable=no` を指定し、`html/body` に `touch-action: none`、`overscroll-behavior: none`、`user-select: none` を設定。さらにドキュメントレベルで `touchmove`・`contextmenu`・`dblclick` を preventDefault。これによりピンチズーム、スクロール、誤操作によるページ遷移を防いでいる。
- **文字を読ませない設計**: 説明文もメニューも設定画面もゲーム内には置かない。絵文字のみを使い、大きなタップ領域と、タップ直後の即座なフィードバック(因果関係がすぐわかる)を重視する。
- **音声合成(TTS)は使わない**: 一度試したが、不自然な発音を繰り返し聞かせるのは望ましくないという理由で明示的に不採用となった。`speechSynthesis` を再導入しないこと。効果音は合成音で代替する。
- **音はWeb Audio APIのみで合成**する。音声ファイルは使わない。`AudioContext` は最初のユーザー操作(ジェスチャー)のタイミングで遅延生成・resumeすること(`ensureAudio()` のパターンを参照)。ブラウザはユーザー操作前の音声再生をブロックするため。また、同じ動作を繰り返しても単調にならないよう、オシレーターの波形や周波数の変化パターンをいくつか用意し、ランダムに切り替えること。
- **視覚エフェクトもランダムに変化**させる。パーティクルの弾け演出はただの `div` を絶対配置し、共通のCSS `@keyframes` を、パーティクルごとのカスタムプロパティ(`--dx`, `--dy`, `--rot`)で駆動する形にしている。1つのkeyframeでも、渡す値の組み合わせ次第で複数の見た目の異なるエフェクトを表現できる。
- **失敗判定やスコアを持たせない**: ゲームループは終わりのない、罰のない設計にする(例: 画面上に同時に存在できる要素数に上限を設け、随時補充し続ける)。3歳半の子どもに適した、勝ち負けのない遊び方。
- **目次への導線**: すべてのゲームに、左上固定・半透明の丸いボタン(`#back-btn`、高い `z-index`)を置き、`index.html` へ直接リンクさせる。新しいゲームでもこのパターン(`env(safe-area-inset-*)` を使った配置も含めて)をそのまま再利用すること。
- **見た目の統一感**: アニメーションするパステルカラーのグラデーション背景(`bgshift` の keyframes)を `index.html` とゲーム画面の両方で使用している。新しいゲームもこの見た目に揃えること。

## ドキュメント記述時の注意

コメント・コミットメッセージ・CLAUDE.mdなどのドキュメントは日本語で書くこと。コード自体の識別子(変数名・関数名など)は対象外で、英語のままでよい。
